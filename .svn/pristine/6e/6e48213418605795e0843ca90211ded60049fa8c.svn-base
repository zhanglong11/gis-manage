import {SET_PROJECT_ID} from "./actions";

export const projectReducer=(state={},action)=>{
    switch (action.type) {
        case SET_PROJECT_ID:
            return {
                ...state,projectId:action.projectId
            }
        default:
            return state;
    }
}